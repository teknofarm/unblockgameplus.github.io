<%
var contentBlock = '<section class="error-page" id="error-page"><div class="container"><div class="error-content">' +
  '<div class="error-code">404</div>' +
  '<h1>Page Not Found</h1>' +
  '<p>' + (typeof description !== 'undefined' ? description : 'The page you are looking for does not exist.') + '</p>' +
  '<div class="error-actions"><a href="/" class="btn-primary">Go Home</a>' +
  '<a href="/search" class="btn-outline">Search Articles</a></div>' +
  '</div></div></section>';

if (typeof schema === 'undefined') var schema = 'search';
%>

<%- include('../layouts/main', { contentBlock: contentBlock }) %>
